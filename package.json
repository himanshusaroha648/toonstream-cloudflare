{
  "name": "toonstream-cloudflare-sync",
  "version": "1.0.0",
  "description": "Toonstream scraper + Supabase uploader - Works on both Node.js and Cloudflare Workers",
  "type": "module",
  "main": "sync-server.js",
  "scripts": {
    "start": "node sync-server.js",
    "sync": "node toonstream-supabase-sync.js",
    "server": "node sync-server.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "proxy:test": "node -e \"import('./proxy-manager.js').then(async ({ default: ProxyManager }) => { const manager = new ProxyManager(); await manager.initialize(); console.log(manager.getStats()); })\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.83.0",
    "axios": "^1.13.2",
    "cheerio": "^1.1.2",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "https-proxy-agent": "^7.0.5",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
